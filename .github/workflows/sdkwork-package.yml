name: SDKWork Package Framework

on:
  workflow_call:
    inputs:
      config_path:
        description: Path to sdkwork.workflow.json in the caller repository.
        required: false
        type: string
        default: sdkwork.workflow.json
      tag:
        description: Git tag, release tag, or source ref to package.
        required: false
        type: string
        default: ''
      package_version:
        description: Package version used in artifact names and manifests.
        required: false
        type: string
        default: ''
      deployment_profile:
        description: Deployment profile filter.
        required: false
        type: string
        default: all
      runtime_target:
        description: Runtime target filter.
        required: false
        type: string
        default: all
      platform:
        description: Platform filter.
        required: false
        type: string
        default: all
      architecture:
        description: Architecture filter.
        required: false
        type: string
        default: all
      profile:
        description: Package profile filter.
        required: false
        type: string
        default: all
      variant:
        description: Package variant filter.
        required: false
        type: string
        default: all
      format:
        description: Package format filter.
        required: false
        type: string
        default: all
      publish_release:
        description: Upload selected artifacts to the caller repository GitHub Release.
        required: false
        type: boolean
        default: true
      upload_artifact:
        description: Upload selected artifacts as workflow artifacts.
        required: false
        type: boolean
        default: true
      retention_days:
        description: Workflow artifact retention days.
        required: false
        type: number
        default: 30
      framework_ref:
        description: sdkwork-github-workflow ref used to checkout framework scripts.
        required: false
        type: string
        default: main
      dependency_refs_json:
        description: JSON object mapping dependency ref input names to refs.
        required: false
        type: string
        default: '{}'
      deploy:
        description: Run configured deployment lifecycle jobs after packaging.
        required: false
        type: boolean
        default: false
      deploy_approval_ref:
        description: Protected-environment approval or change-record reference for deploy apply.
        required: false
        type: string
        default: ''
      deploy_rollback_target:
        description: Immutable rollback target or approved forward-fix boundary for deploy apply.
        required: false
        type: string
        default: ''
    secrets:
      SDKWORK_RELEASE_TOKEN:
        description: Token used for private dependency checkouts and optional release upload.
        required: false
      SDKWORK_RELEASE_SIGNING_PRIVATE_KEY:
        description: PEM private key used by application detached-signature lifecycle hooks.
        required: false
      SDKWORK_RELEASE_SIGNING_PRIVATE_KEY_PASSWORD:
        description: Optional password for the detached-signature private key.
        required: false
      SDKWORK_ANDROID_KEYSTORE_BASE64:
        description: Base64-encoded Android release keystore.
        required: false
      SDKWORK_ANDROID_KEYSTORE_PASSWORD:
        description: Android release keystore password.
        required: false
      SDKWORK_ANDROID_KEY_ALIAS:
        description: Android release key alias.
        required: false
      SDKWORK_ANDROID_KEY_PASSWORD:
        description: Android release key password.
        required: false

permissions:
  contents: write
  actions: read
  id-token: write
  attestations: write
  artifact-metadata: write

concurrency:
  group: sdkwork-package-${{ github.repository }}-${{ inputs.tag || github.ref_name }}-${{ inputs.deployment_profile }}-${{ inputs.runtime_target }}-${{ inputs.platform }}-${{ inputs.architecture }}-${{ inputs.profile }}-${{ inputs.variant }}-${{ inputs.format }}
  cancel-in-progress: false

jobs:
  plan:
    name: Plan package matrix
    runs-on: ubuntu-24.04
    outputs:
      matrix: ${{ steps.plan.outputs.matrix }}
      target_count: ${{ steps.plan.outputs.target_count }}
      summary_json: ${{ steps.plan.outputs.summary_json }}
      dependencies: ${{ steps.dependencies.outputs.dependencies }}
      dependency_count: ${{ steps.dependencies.outputs.dependency_count }}
      deployments: ${{ steps.deployments.outputs.deployments }}
      deployment_count: ${{ steps.deployments.outputs.deployment_count }}
    steps:
      - name: Checkout application source
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag || github.ref }}
          fetch-depth: 0
          submodules: recursive
          token: ${{ secrets.SDKWORK_RELEASE_TOKEN || github.token }}

      - name: Checkout workflow framework
        uses: actions/checkout@v4
        with:
          repository: sdkwork-ai/sdkwork-github-workflow
          ref: ${{ inputs.framework_ref }}
          path: .sdkwork/github-workflow
          fetch-depth: 1
          token: ${{ secrets.SDKWORK_RELEASE_TOKEN || github.token }}

      - id: validate
        name: Validate workflow config
        uses: ./.sdkwork/github-workflow/actions/validate-config
        with:
          config-path: ${{ inputs.config_path }}

      - id: plan
        name: Resolve package matrix
        shell: bash
        env:
          SDKWORK_CONFIG_PATH: ${{ inputs.config_path }}
          SDKWORK_FILTER_DEPLOYMENT_PROFILE: ${{ inputs.deployment_profile }}
          SDKWORK_FILTER_RUNTIME_TARGET: ${{ inputs.runtime_target }}
          SDKWORK_FILTER_PLATFORM: ${{ inputs.platform }}
          SDKWORK_FILTER_ARCHITECTURE: ${{ inputs.architecture }}
          SDKWORK_FILTER_PROFILE: ${{ inputs.profile }}
          SDKWORK_FILTER_VARIANT: ${{ inputs.variant }}
          SDKWORK_FILTER_FORMAT: ${{ inputs.format }}
          SDKWORK_PACKAGE_VERSION: ${{ inputs.package_version }}
          SDKWORK_RELEASE_TAG: ${{ inputs.tag || github.ref_name }}
        run: |
          set -euo pipefail
          node .sdkwork/github-workflow/scripts/sdkwork-workflow.mjs matrix \
            --config "${SDKWORK_CONFIG_PATH}" \
            --deployment-profile "${SDKWORK_FILTER_DEPLOYMENT_PROFILE}" \
            --runtime-target "${SDKWORK_FILTER_RUNTIME_TARGET}" \
            --platform "${SDKWORK_FILTER_PLATFORM}" \
            --architecture "${SDKWORK_FILTER_ARCHITECTURE}" \
            --profile "${SDKWORK_FILTER_PROFILE}" \
            --variant "${SDKWORK_FILTER_VARIANT}" \
            --format "${SDKWORK_FILTER_FORMAT}" \
            --version "${SDKWORK_PACKAGE_VERSION}" \
            --release-tag "${SDKWORK_RELEASE_TAG}" \
            --github-output

      - id: dependencies
        name: Resolve dependency checkout plan
        shell: bash
        env:
          SDKWORK_CONFIG_PATH: ${{ inputs.config_path }}
          SDKWORK_DEPENDENCY_REFS_JSON: ${{ inputs.dependency_refs_json }}
        run: |
          set -euo pipefail
          node .sdkwork/github-workflow/scripts/sdkwork-workflow.mjs dependencies \
            --config "${SDKWORK_CONFIG_PATH}" \
            --dependency-refs-json "${SDKWORK_DEPENDENCY_REFS_JSON}" \
            --github-output

      - id: deployments
        name: Resolve deployment matrix
        shell: bash
        env:
          SDKWORK_CONFIG_PATH: ${{ inputs.config_path }}
          SDKWORK_FILTER_DEPLOYMENT_PROFILE: ${{ inputs.deployment_profile }}
          SDKWORK_FILTER_RUNTIME_TARGET: ${{ inputs.runtime_target }}
          SDKWORK_FILTER_PLATFORM: ${{ inputs.platform }}
          SDKWORK_FILTER_ARCHITECTURE: ${{ inputs.architecture }}
          SDKWORK_FILTER_PROFILE: ${{ inputs.profile }}
          SDKWORK_FILTER_VARIANT: ${{ inputs.variant }}
          SDKWORK_FILTER_FORMAT: ${{ inputs.format }}
        run: |
          set -euo pipefail
          node .sdkwork/github-workflow/scripts/sdkwork-workflow.mjs deployments \
            --config "${SDKWORK_CONFIG_PATH}" \
            --deployment-profile "${SDKWORK_FILTER_DEPLOYMENT_PROFILE}" \
            --runtime-target "${SDKWORK_FILTER_RUNTIME_TARGET}" \
            --platform "${SDKWORK_FILTER_PLATFORM}" \
            --architecture "${SDKWORK_FILTER_ARCHITECTURE}" \
            --profile "${SDKWORK_FILTER_PROFILE}" \
            --variant "${SDKWORK_FILTER_VARIANT}" \
            --format "${SDKWORK_FILTER_FORMAT}" \
            --github-output

      - name: Require package artifacts for deployment
        if: ${{ inputs.deploy && (!inputs.upload_artifact || !fromJson(steps.matrix.outputs.summary_json).publish.workflowArtifact) }}
        shell: bash
        run: |
          echo "Deployment requires workflow artifact upload from the selected package jobs." >&2
          exit 1

      - name: Require a selected deployment
        if: ${{ inputs.deploy && steps.deployments.outputs.deployment_count == '0' }}
        shell: bash
        run: |
          echo "Deployment was requested but no deployment matched the selected package matrix." >&2
          exit 1

      - name: Require package artifacts for aggregate release
        if: ${{ inputs.publish_release && fromJson(steps.matrix.outputs.summary_json).publish.aggregateRelease && (!inputs.upload_artifact || !fromJson(steps.matrix.outputs.summary_json).publish.workflowArtifact) }}
        shell: bash
        run: |
          echo "Aggregate release requires workflow artifact upload from package jobs." >&2
          exit 1

  package:
    name: Package ${{ matrix.packageId }}
    needs: plan
    if: needs.plan.outputs.target_count != '0'
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.plan.outputs.matrix) }}
    env:
      SDKWORK_RELEASE_TAG: ${{ inputs.tag || github.ref_name }}
      SDKWORK_PACKAGE_VERSION: ${{ inputs.package_version }}
      SDKWORK_PACKAGE_ID: ${{ matrix.packageId }}
      SDKWORK_PACKAGE_TARGET_ID: ${{ matrix.id }}
      SDKWORK_DEPLOYMENT_PROFILE: ${{ matrix.deploymentProfile }}
      SDKWORK_SUPPORTED_DEPLOYMENT_PROFILES: ${{ join(matrix.supportedDeploymentProfiles, ',') }}
      SDKWORK_RUNTIME_TARGET: ${{ matrix.runtimeTarget }}
      SDKWORK_TARGET_PLATFORM: ${{ matrix.targetPlatform }}
      SDKWORK_CLIENT_ARCHITECTURE: ${{ matrix.clientArchitecture }}
      SDKWORK_PACKAGE_PROFILE: ${{ matrix.profile }}
      SDKWORK_PACKAGE_PLATFORM: ${{ matrix.platform }}
      SDKWORK_PACKAGE_ARCHITECTURE: ${{ matrix.architecture }}
      SDKWORK_PACKAGE_VARIANT: ${{ matrix.variant }}
      SDKWORK_PACKAGE_FORMAT: ${{ matrix.format }}
      GH_TOKEN: ${{ secrets.SDKWORK_RELEASE_TOKEN || github.token }}
      SDKWORK_RELEASE_SIGNING_PRIVATE_KEY: ${{ secrets.SDKWORK_RELEASE_SIGNING_PRIVATE_KEY }}
      SDKWORK_RELEASE_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.SDKWORK_RELEASE_SIGNING_PRIVATE_KEY_PASSWORD }}
      SDKWORK_ANDROID_KEYSTORE_BASE64: ${{ secrets.SDKWORK_ANDROID_KEYSTORE_BASE64 }}
      SDKWORK_ANDROID_KEYSTORE_PASSWORD: ${{ secrets.SDKWORK_ANDROID_KEYSTORE_PASSWORD }}
      SDKWORK_ANDROID_KEY_ALIAS: ${{ secrets.SDKWORK_ANDROID_KEY_ALIAS }}
      SDKWORK_ANDROID_KEY_PASSWORD: ${{ secrets.SDKWORK_ANDROID_KEY_PASSWORD }}
    steps:
      - name: Checkout application source
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag || github.ref }}
          fetch-depth: 0
          submodules: recursive
          token: ${{ secrets.SDKWORK_RELEASE_TOKEN || github.token }}

      - name: Checkout workflow framework
        uses: actions/checkout@v4
        with:
          repository: sdkwork-ai/sdkwork-github-workflow
          ref: ${{ inputs.framework_ref }}
          path: .sdkwork/github-workflow
          fetch-depth: 1
          token: ${{ secrets.SDKWORK_RELEASE_TOKEN || github.token }}

      - name: Checkout declared dependencies
        uses: ./.sdkwork/github-workflow/actions/checkout-dependencies
        with:
          dependencies-json: ${{ needs.plan.outputs.dependencies }}
          token: ${{ secrets.SDKWORK_RELEASE_TOKEN || github.token }}

      - name: Set up declared toolchains
        uses: ./.sdkwork/github-workflow/actions/setup-toolchains
        with:
          config-path: ${{ inputs.config_path }}

      - name: Preflight
        uses: ./.sdkwork/github-workflow/actions/run-lifecycle
        with:
          config-path: ${{ inputs.config_path }}
          phase: preflight
          target-id: ${{ matrix.id }}
          deployment-profile: ${{ matrix.deploymentProfile }}
          runtime-target: ${{ matrix.runtimeTarget }}
          platform: ${{ matrix.platform }}
          architecture: ${{ matrix.architecture }}
          profile: ${{ matrix.profile }}
          format: ${{ matrix.format }}
          version: ${{ inputs.package_version }}
          release-tag: ${{ inputs.tag || github.ref_name }}
          deploy-environment: ${{ matrix.environment }}
          deploy-url: ${{ matrix.url }}
          deploy-lifecycle: ${{ matrix.lifecycle }}

      - name: Install
        uses: ./.sdkwork/github-workflow/actions/run-lifecycle
        with:
          config-path: ${{ inputs.config_path }}
          phase: install
          target-id: ${{ matrix.id }}
          deployment-profile: ${{ matrix.deploymentProfile }}
          runtime-target: ${{ matrix.runtimeTarget }}
          platform: ${{ matrix.platform }}
          architecture: ${{ matrix.architecture }}
          profile: ${{ matrix.profile }}
          format: ${{ matrix.format }}
          version: ${{ inputs.package_version }}
          release-tag: ${{ inputs.tag || github.ref_name }}

      - name: Build
        uses: ./.sdkwork/github-workflow/actions/run-lifecycle
        with:
          config-path: ${{ inputs.config_path }}
          phase: build
          target-id: ${{ matrix.id }}
          deployment-profile: ${{ matrix.deploymentProfile }}
          runtime-target: ${{ matrix.runtimeTarget }}
          platform: ${{ matrix.platform }}
          architecture: ${{ matrix.architecture }}
          profile: ${{ matrix.profile }}
          format: ${{ matrix.format }}
          version: ${{ inputs.package_version }}
          release-tag: ${{ inputs.tag || github.ref_name }}

      - name: Stage
        uses: ./.sdkwork/github-workflow/actions/run-lifecycle
        with:
          config-path: ${{ inputs.config_path }}
          phase: stage
          target-id: ${{ matrix.id }}
          deployment-profile: ${{ matrix.deploymentProfile }}
          runtime-target: ${{ matrix.runtimeTarget }}
          platform: ${{ matrix.platform }}
          architecture: ${{ matrix.architecture }}
          profile: ${{ matrix.profile }}
          format: ${{ matrix.format }}
          version: ${{ inputs.package_version }}
          release-tag: ${{ inputs.tag || github.ref_name }}

      - name: Package
        uses: ./.sdkwork/github-workflow/actions/run-lifecycle
        with:
          config-path: ${{ inputs.config_path }}
          phase: package
          target-id: ${{ matrix.id }}
          deployment-profile: ${{ matrix.deploymentProfile }}
          runtime-target: ${{ matrix.runtimeTarget }}
          platform: ${{ matrix.platform }}
          architecture: ${{ matrix.architecture }}
          profile: ${{ matrix.profile }}
          format: ${{ matrix.format }}
          version: ${{ inputs.package_version }}
          release-tag: ${{ inputs.tag || github.ref_name }}

      - name: Sign artifacts
        uses: ./.sdkwork/github-workflow/actions/run-lifecycle
        with:
          config-path: ${{ inputs.config_path }}
          phase: sign
          target-id: ${{ matrix.id }}
          deployment-profile: ${{ matrix.deploymentProfile }}
          runtime-target: ${{ matrix.runtimeTarget }}
          platform: ${{ matrix.platform }}
          architecture: ${{ matrix.architecture }}
          profile: ${{ matrix.profile }}
          format: ${{ matrix.format }}
          version: ${{ inputs.package_version }}
          release-tag: ${{ inputs.tag || github.ref_name }}

      - name: Generate SBOM
        uses: ./.sdkwork/github-workflow/actions/run-lifecycle
        with:
          config-path: ${{ inputs.config_path }}
          phase: sbom
          target-id: ${{ matrix.id }}
          deployment-profile: ${{ matrix.deploymentProfile }}
          runtime-target: ${{ matrix.runtimeTarget }}
          platform: ${{ matrix.platform }}
          architecture: ${{ matrix.architecture }}
          profile: ${{ matrix.profile }}
          format: ${{ matrix.format }}
          version: ${{ inputs.package_version }}
          release-tag: ${{ inputs.tag || github.ref_name }}

      - name: Validate artifacts
        uses: ./.sdkwork/github-workflow/actions/run-lifecycle
        with:
          config-path: ${{ inputs.config_path }}
          phase: validate
          target-id: ${{ matrix.id }}
          deployment-profile: ${{ matrix.deploymentProfile }}
          runtime-target: ${{ matrix.runtimeTarget }}
          platform: ${{ matrix.platform }}
          architecture: ${{ matrix.architecture }}
          profile: ${{ matrix.profile }}
          format: ${{ matrix.format }}
          version: ${{ inputs.package_version }}
          release-tag: ${{ inputs.tag || github.ref_name }}

      - name: Verify package artifact evidence
        if: ${{ matrix.deployable }}
        shell: bash
        env:
          SDKWORK_CONFIG_PATH: ${{ inputs.config_path }}
          SDKWORK_TARGET_ID: ${{ matrix.id }}
          SDKWORK_TARGET_DEPLOYMENT_PROFILE: ${{ matrix.deploymentProfile }}
          SDKWORK_TARGET_RUNTIME_TARGET: ${{ matrix.runtimeTarget }}
          SDKWORK_TARGET_PLATFORM: ${{ matrix.platform }}
          SDKWORK_TARGET_ARCHITECTURE: ${{ matrix.architecture }}
          SDKWORK_TARGET_PROFILE: ${{ matrix.profile }}
          SDKWORK_TARGET_FORMAT: ${{ matrix.format }}
          SDKWORK_PACKAGE_VERSION_INPUT: ${{ inputs.package_version }}
          SDKWORK_RELEASE_TAG_INPUT: ${{ inputs.tag || github.ref_name }}
          SDKWORK_ARTIFACT_EVIDENCE_PATHS: ${{ matrix.artifactEvidencePathsText }}
        run: |
          set -euo pipefail
          SDKWORK_SOURCE_COMMIT="$(git rev-parse HEAD)"
          while IFS= read -r evidence_path; do
            if [ -z "${evidence_path}" ]; then
              continue
            fi
            node .sdkwork/github-workflow/scripts/sdkwork-workflow.mjs evidence \
              --config "${SDKWORK_CONFIG_PATH}" \
              --target-id "${SDKWORK_TARGET_ID}" \
              --deployment-profile "${SDKWORK_TARGET_DEPLOYMENT_PROFILE}" \
              --runtime-target "${SDKWORK_TARGET_RUNTIME_TARGET}" \
              --platform "${SDKWORK_TARGET_PLATFORM}" \
              --architecture "${SDKWORK_TARGET_ARCHITECTURE}" \
              --profile "${SDKWORK_TARGET_PROFILE}" \
              --format "${SDKWORK_TARGET_FORMAT}" \
              --version "${SDKWORK_PACKAGE_VERSION_INPUT}" \
              --release-tag "${SDKWORK_RELEASE_TAG_INPUT}" \
              --source-commit "${SDKWORK_SOURCE_COMMIT}" \
              --artifact-root . \
              --artifact-evidence "${evidence_path}"
          done <<< "${SDKWORK_ARTIFACT_EVIDENCE_PATHS}"

      - name: Upload workflow artifact
        if: ${{ inputs.upload_artifact && fromJson(needs.plan.outputs.summary_json).publish.workflowArtifact }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.workflowArtifactName }}-${{ inputs.tag || github.run_number }}
          path: ${{ matrix.artifactUploadGlobsText }}
          if-no-files-found: error
          retention-days: ${{ fromJson(needs.plan.outputs.summary_json).publish.retentionDays || inputs.retention_days }}

      - name: Attest build provenance
        if: ${{ fromJson(needs.plan.outputs.summary_json).security.artifactAttestations }}
        uses: actions/attest@v4
        with:
          subject-path: ${{ matrix.outputGlobsText }}

      - id: changelog
        name: Render release changelog
        if: ${{ inputs.publish_release && matrix.deployable && fromJson(needs.plan.outputs.summary_json).publish.githubRelease && !fromJson(needs.plan.outputs.summary_json).publish.aggregateRelease }}
        shell: bash
        env:
          SDKWORK_CONFIG_PATH: ${{ inputs.config_path }}
          SDKWORK_FILTER_DEPLOYMENT_PROFILE: ${{ inputs.deployment_profile }}
          SDKWORK_FILTER_RUNTIME_TARGET: ${{ inputs.runtime_target }}
          SDKWORK_FILTER_PLATFORM: ${{ inputs.platform }}
          SDKWORK_FILTER_ARCHITECTURE: ${{ inputs.architecture }}
          SDKWORK_FILTER_PROFILE: ${{ inputs.profile }}
          SDKWORK_FILTER_VARIANT: ${{ inputs.variant }}
          SDKWORK_FILTER_FORMAT: ${{ inputs.format }}
          SDKWORK_PACKAGE_VERSION: ${{ inputs.package_version }}
          SDKWORK_RELEASE_TAG: ${{ inputs.tag || github.ref_name }}
        run: |
          set -euo pipefail
          node .sdkwork/github-workflow/scripts/sdkwork-workflow.mjs changelog \
            --config "${SDKWORK_CONFIG_PATH}" \
            --deployment-profile "${SDKWORK_FILTER_DEPLOYMENT_PROFILE}" \
            --runtime-target "${SDKWORK_FILTER_RUNTIME_TARGET}" \
            --platform "${SDKWORK_FILTER_PLATFORM}" \
            --architecture "${SDKWORK_FILTER_ARCHITECTURE}" \
            --profile "${SDKWORK_FILTER_PROFILE}" \
            --variant "${SDKWORK_FILTER_VARIANT}" \
            --format "${SDKWORK_FILTER_FORMAT}" \
            --version "${SDKWORK_PACKAGE_VERSION}" \
            --release-tag "${SDKWORK_RELEASE_TAG}" \
            --output .sdkwork/release/release-notes.md \
            --github-output

      - name: Upload assets to GitHub Release
        if: ${{ inputs.publish_release && matrix.deployable && fromJson(needs.plan.outputs.summary_json).publish.githubRelease && !fromJson(needs.plan.outputs.summary_json).publish.aggregateRelease }}
        uses: ./.sdkwork/github-workflow/actions/publish-release
        with:
          tag: ${{ inputs.tag || github.ref_name }}
          repository: ${{ github.repository }}
          files: ${{ matrix.artifactUploadGlobsText }}
          notes-file: ${{ steps.changelog.outputs.notes_path }}
          clobber: 'true'
        env:
          GH_TOKEN: ${{ secrets.SDKWORK_RELEASE_TOKEN || github.token }}

  publish:
    name: Publish aggregate GitHub release
    needs:
      - plan
      - package
    if: ${{ inputs.publish_release && fromJson(needs.plan.outputs.summary_json).publishableTargets > 0 && fromJson(needs.plan.outputs.summary_json).publish.githubRelease && fromJson(needs.plan.outputs.summary_json).publish.aggregateRelease }}
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    env:
      SDKWORK_RELEASE_TAG: ${{ inputs.tag || github.ref_name }}
      SDKWORK_PACKAGE_VERSION: ${{ inputs.package_version }}
      SDKWORK_PACKAGE_ID: aggregate-release
      SDKWORK_PACKAGE_TARGET_ID: aggregate-release
      SDKWORK_PACKAGE_PROFILE: library
      SDKWORK_PACKAGE_PLATFORM: web
      SDKWORK_PACKAGE_ARCHITECTURE: noarch
      SDKWORK_PACKAGE_FORMAT: zip
      GH_TOKEN: ${{ secrets.SDKWORK_RELEASE_TOKEN || github.token }}
    steps:
      - name: Checkout application source
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag || github.ref }}
          fetch-depth: 0
          submodules: recursive
          token: ${{ secrets.SDKWORK_RELEASE_TOKEN || github.token }}

      - name: Checkout workflow framework
        uses: actions/checkout@v4
        with:
          repository: sdkwork-ai/sdkwork-github-workflow
          ref: ${{ inputs.framework_ref }}
          path: .sdkwork/github-workflow
          fetch-depth: 1
          token: ${{ secrets.SDKWORK_RELEASE_TOKEN || github.token }}

      - name: Set up declared toolchains
        uses: ./.sdkwork/github-workflow/actions/setup-toolchains
        with:
          config-path: ${{ inputs.config_path }}

      - name: Download aggregate package artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: ${{ fromJson(needs.plan.outputs.summary_json).publish.aggregateArtifactPattern }}
          path: ${{ fromJson(needs.plan.outputs.summary_json).publish.aggregateArtifactPath }}
          merge-multiple: true

      - name: Run aggregate publish lifecycle
        uses: ./.sdkwork/github-workflow/actions/run-lifecycle
        with:
          config-path: ${{ inputs.config_path }}
          phase: publish
          target-id: ${{ fromJson(needs.plan.outputs.matrix).include[0].id }}
          deployment-profile: ${{ fromJson(needs.plan.outputs.matrix).include[0].deploymentProfile }}
          runtime-target: ${{ fromJson(needs.plan.outputs.matrix).include[0].runtimeTarget }}
          platform: ${{ fromJson(needs.plan.outputs.matrix).include[0].platform }}
          architecture: ${{ fromJson(needs.plan.outputs.matrix).include[0].architecture }}
          profile: ${{ fromJson(needs.plan.outputs.matrix).include[0].profile }}
          format: ${{ fromJson(needs.plan.outputs.matrix).include[0].format }}
          version: ${{ inputs.package_version }}
          release-tag: ${{ inputs.tag || github.ref_name }}
          aggregate-release: 'true'

      - id: changelog
        name: Render release changelog
        shell: bash
        env:
          SDKWORK_CONFIG_PATH: ${{ inputs.config_path }}
          SDKWORK_FILTER_DEPLOYMENT_PROFILE: ${{ inputs.deployment_profile }}
          SDKWORK_FILTER_RUNTIME_TARGET: ${{ inputs.runtime_target }}
          SDKWORK_FILTER_PLATFORM: ${{ inputs.platform }}
          SDKWORK_FILTER_ARCHITECTURE: ${{ inputs.architecture }}
          SDKWORK_FILTER_PROFILE: ${{ inputs.profile }}
          SDKWORK_FILTER_VARIANT: ${{ inputs.variant }}
          SDKWORK_FILTER_FORMAT: ${{ inputs.format }}
          SDKWORK_PACKAGE_VERSION: ${{ inputs.package_version }}
          SDKWORK_RELEASE_TAG: ${{ inputs.tag || github.ref_name }}
        run: |
          set -euo pipefail
          node .sdkwork/github-workflow/scripts/sdkwork-workflow.mjs changelog \
            --config "${SDKWORK_CONFIG_PATH}" \
            --deployment-profile "${SDKWORK_FILTER_DEPLOYMENT_PROFILE}" \
            --runtime-target "${SDKWORK_FILTER_RUNTIME_TARGET}" \
            --platform "${SDKWORK_FILTER_PLATFORM}" \
            --architecture "${SDKWORK_FILTER_ARCHITECTURE}" \
            --profile "${SDKWORK_FILTER_PROFILE}" \
            --variant "${SDKWORK_FILTER_VARIANT}" \
            --format "${SDKWORK_FILTER_FORMAT}" \
            --version "${SDKWORK_PACKAGE_VERSION}" \
            --release-tag "${SDKWORK_RELEASE_TAG}" \
            --output .sdkwork/release/release-notes.md \
            --github-output

      - name: Upload aggregate assets to GitHub Release
        uses: ./.sdkwork/github-workflow/actions/publish-release
        with:
          tag: ${{ inputs.tag || github.ref_name }}
          repository: ${{ github.repository }}
          files: ${{ fromJson(needs.plan.outputs.summary_json).publish.aggregateUploadGlobsText }}
          notes-file: ${{ steps.changelog.outputs.notes_path }}
          clobber: 'true'
        env:
          GH_TOKEN: ${{ secrets.SDKWORK_RELEASE_TOKEN || github.token }}

  deploy:
    name: Deploy ${{ matrix.packageId }} to ${{ matrix.environment }}
    needs:
      - plan
      - package
    if: inputs.deploy && needs.plan.outputs.deployment_count != '0'
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.plan.outputs.deployments) }}
    environment:
      name: ${{ matrix.environment }}
      url: ${{ matrix.url }}
    env:
      SDKWORK_RELEASE_TAG: ${{ inputs.tag || github.ref_name }}
      SDKWORK_PACKAGE_VERSION: ${{ inputs.package_version }}
      SDKWORK_PACKAGE_ID: ${{ matrix.packageId }}
      SDKWORK_PACKAGE_TARGET_ID: ${{ matrix.targetId }}
      SDKWORK_DEPLOYMENT_PROFILE: ${{ matrix.deploymentProfile }}
      SDKWORK_SUPPORTED_DEPLOYMENT_PROFILES: ${{ join(matrix.supportedDeploymentProfiles, ',') }}
      SDKWORK_RUNTIME_TARGET: ${{ matrix.runtimeTarget }}
      SDKWORK_PACKAGE_PROFILE: ${{ matrix.profile }}
      SDKWORK_PACKAGE_PLATFORM: ${{ matrix.platform }}
      SDKWORK_PACKAGE_ARCHITECTURE: ${{ matrix.architecture }}
      SDKWORK_PACKAGE_VARIANT: ${{ matrix.variant }}
      SDKWORK_PACKAGE_FORMAT: ${{ matrix.format }}
      SDKWORK_DEPLOY_ENVIRONMENT: ${{ matrix.environment }}
      SDKWORK_DEPLOY_URL: ${{ matrix.url }}
      SDKWORK_DEPLOY_APPROVAL_REF: ${{ inputs.deploy_approval_ref }}
      SDKWORK_DEPLOY_ROLLBACK_TARGET: ${{ inputs.deploy_rollback_target }}
      SDKWORK_ARTIFACT_EVIDENCE_PATH: ${{ format('.sdkwork/artifacts/{0}', matrix.artifactEvidencePath) }}
      GH_TOKEN: ${{ secrets.SDKWORK_RELEASE_TOKEN || github.token }}
    steps:
      - name: Checkout application source
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag || github.ref }}
          fetch-depth: 0
          submodules: recursive
          token: ${{ secrets.SDKWORK_RELEASE_TOKEN || github.token }}

      - name: Checkout workflow framework
        uses: actions/checkout@v4
        with:
          repository: sdkwork-ai/sdkwork-github-workflow
          ref: ${{ inputs.framework_ref }}
          path: .sdkwork/github-workflow
          fetch-depth: 1
          token: ${{ secrets.SDKWORK_RELEASE_TOKEN || github.token }}

      - name: Set up declared toolchains
        uses: ./.sdkwork/github-workflow/actions/setup-toolchains
        with:
          config-path: ${{ inputs.config_path }}

      - name: Download package artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: 'sdkwork-publishable-*${{ matrix.packageId }}*'
          path: .sdkwork/artifacts
          merge-multiple: true

      - name: Verify artifact evidence
        if: ${{ matrix.artifactEvidencePath != '' }}
        shell: bash
        env:
          SDKWORK_CONFIG_PATH: ${{ inputs.config_path }}
          SDKWORK_TARGET_ID: ${{ matrix.targetId }}
          SDKWORK_TARGET_DEPLOYMENT_PROFILE: ${{ matrix.deploymentProfile }}
          SDKWORK_TARGET_RUNTIME_TARGET: ${{ matrix.runtimeTarget }}
          SDKWORK_TARGET_PLATFORM: ${{ matrix.platform }}
          SDKWORK_TARGET_ARCHITECTURE: ${{ matrix.architecture }}
          SDKWORK_TARGET_PROFILE: ${{ matrix.profile }}
          SDKWORK_TARGET_FORMAT: ${{ matrix.format }}
          SDKWORK_DEPLOY_ENVIRONMENT_INPUT: ${{ matrix.environment }}
          SDKWORK_ARTIFACT_EVIDENCE_INPUT: ${{ format('.sdkwork/artifacts/{0}', matrix.artifactEvidencePath) }}
        run: |
          set -euo pipefail
          node .sdkwork/github-workflow/scripts/sdkwork-workflow.mjs evidence \
            --config "${SDKWORK_CONFIG_PATH}" \
            --target-id "${SDKWORK_TARGET_ID}" \
            --deployment-profile "${SDKWORK_TARGET_DEPLOYMENT_PROFILE}" \
            --runtime-target "${SDKWORK_TARGET_RUNTIME_TARGET}" \
            --platform "${SDKWORK_TARGET_PLATFORM}" \
            --architecture "${SDKWORK_TARGET_ARCHITECTURE}" \
            --profile "${SDKWORK_TARGET_PROFILE}" \
            --format "${SDKWORK_TARGET_FORMAT}" \
            --deploy-environment "${SDKWORK_DEPLOY_ENVIRONMENT_INPUT}" \
            --version "${SDKWORK_PACKAGE_VERSION}" \
            --release-tag "${SDKWORK_RELEASE_TAG}" \
            --source-commit "$(git rev-parse HEAD)" \
            --artifact-root .sdkwork/artifacts \
            --artifact-evidence "${SDKWORK_ARTIFACT_EVIDENCE_INPUT}"

      - name: Run deployment lifecycle
        uses: ./.sdkwork/github-workflow/actions/run-lifecycle
        with:
          config-path: ${{ inputs.config_path }}
          phase: ${{ matrix.lifecycle }}
          target-id: ${{ matrix.targetId }}
          deployment-profile: ${{ matrix.deploymentProfile }}
          runtime-target: ${{ matrix.runtimeTarget }}
          platform: ${{ matrix.platform }}
          architecture: ${{ matrix.architecture }}
          profile: ${{ matrix.profile }}
          format: ${{ matrix.format }}
          version: ${{ inputs.package_version }}
          release-tag: ${{ inputs.tag || github.ref_name }}
          deploy-environment: ${{ matrix.environment }}
          deploy-url: ${{ matrix.url }}
          deploy-lifecycle: ${{ matrix.lifecycle }}
